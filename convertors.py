# this is a new module to be used with modules.py
# as we write more calsses and functions we put them in corresponding modules. This is one such
# this module will be imported into the modules.py program
def lbs_to_kg(weight):
    return weight * 0.45


def kg_to_lbs(weight):
    return weight / 0.45
